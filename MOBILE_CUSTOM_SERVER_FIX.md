# 🔧 حل مشاكل Custom Server للموبايل

## المشاكل المكتشفة:

### 1. ✅ **مكتبة Vector Icons مفقودة** (تم الحل)
```
❌ '@react-native-vector-icons/material-design-icons' not found
✅ تم التثبيت: npm install @react-native-vector-icons/material-design-icons --save
```

### 2. ⚠️ **403 Forbidden عند الوصول للـ API**
السبب: الموبايل يحاول الوصول إلى `http://localhost:8080` وهو لا يعمل على المتصفح الويب

**الحل:**
استخدم عنوان IP الجهاز بدلاً من localhost:
- بدلاً من: `http://localhost:8080`
- استخدم: `http://192.168.1.X:8080` (حيث X رقم جهازك)

### 3. ⚠️ **React 18 Warning**
الملف `mobile/index.tsx` يستخدم `ReactDOM.render` القديم

### 4. ⚠️ **Lazy Loading Error**
المكونات توقفت أثناء التحميل المتزامن

---

## الخطوات الصحيحة:

### أ) إعادة تشغيل الموبايل بعد تثبيت المكتبة:
```bash
cd /home/zakee/erp systems/atlas/cmms/mobile
npm start --web --clear
```

### ب) في شاشة تسجيل الدخول، عند الضغط على "Custom Server":
```
ادخل URL الخادم:
- للاختبار المحلي: http://192.168.X.X:8080 (استبدل X برقم جهازك)
- للإنتاج: https://your-domain.com:8080
```

### ج) تحقق من أن:
- ✅ الـ Backend يعمل على البورت 8080
- ✅ الـ Frontend يعمل على البورت 3000
- ✅ MinIO يعمل على البورت 9000
- ✅ استخدام نفس الشبكة (يفضل الـ Ethernet إذا أمكن)

---

## كيفية العثور على IP جهازك:

### على Linux/Mac:
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
```

### على Windows:
```bash
ipconfig
```

ابحث عن IPv4 Address في شبكتك المحلية (عادة تبدأ بـ 192.168. أو 10.)

---

## الملفات المتعلقة:
- ✅ `/cmms/mobile/config.ts` - إعدادات API
- ✅ `/cmms/api/src/main/java/com/grash/configuration/WebMvcConfig.java` - CORS معد بشكل صحيح
- ✅ `/cmms/mobile/screens/auth/CustomServerScreen.tsx` - شاشة Custom Server

