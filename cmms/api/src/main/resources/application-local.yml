spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/atlas
    username: rootuser
    password: 5525
    driver-class-name: org.postgresql.Driver
  jackson:
    serialization:
      fail-on-empty-beans: false
  liquibase:
    change-log: classpath:/db/master.xml
    enabled: true
  messages:
    encoding: UTF-8
  servlet:
    multipart:
      max-request-size: 20MB
      max-file-size: 20MB
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        enable_lazy_load_no_trans: true
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        id:
          new_generator_mappings: false
    defer-datasource-initialization: false
    properties.mail.smtp:
      auth: true
      starttls.enable: true
      ssl.trust: smtp.gmail.com
  sql:
    init:
      mode: always
  mail:
    host: smtp.gmail.com
    port: 587
    username: 
    password: 
    protocol: smtp
    tls: true
    properties.mail.smtp:
      auth: true
      starttls.enable: true
      ssl.trust: smtp.gmail.com
  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=1000,expireAfterWrite=20m

server:
  port: 8080
  servlet:
    context-path: /
  error:
    include-message: always
    include-binding-errors: always

# CORS configuration
cors:
  allowed-origins: http://localhost:3000,http://localhost:3001
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS,PATCH
  allowed-headers: "*"
  allow-credentials: true

security:
  jwt:
    token:
      secret-key: xK8mP9qR2sT5vW7yA3bC6dE9fG2jH5kN8pQ
      expire-length: 1209600000

frontend:
  url: http://localhost:3000

mail:
  recipients: 
  enable: false

api:
  host: http://localhost:8080

storage:
  type: LOCAL

allowed-organization-admins: 
cloud-version: false
enable-sso: false
oauth2:
  success-redirect-url: http://localhost:3000/oauth2/success
  failure-redirect-url: http://localhost:3000/oauth2/failure
  provider: 

license-key: 
license-fingerprint-required: false

white-labeling:
  logo-paths: 
  custom-colors: 
  brand-config: 
